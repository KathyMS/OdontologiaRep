{% include 'AdministradorBundle:Default:administrador_header.html.twig' %}
{% include 'AdministradorBundle:Default:administrador_menu_horizontal.html.twig' %}

     <div class="box col-md-12">
        <div class="box-inner">
            <div class="box-header well" data-original-title="">
                <h2>Consentimiento Informado</h2>

            </div>
    <div class="box-content">
    <table class="table table-bordered table-striped table-condensed">
        <thead>
            <tr>
                <th>Num</th>
                <th>Rutaimagen</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('consentimientoinformado_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.rutaImagen }}</td>
                <td>
                <ul>
                  
                    <a href="{{ path('consentimientoinformado_show', { 'id': entity.id }) }}" class="btn btn-primary btn-sm">Ver</a>
                   
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
 <center>
                <ul>

                    <a href="{{ path('consentimientoinformado_new') }}">
             <strong>Nuevo consentimiento      </strong>      </a>

                </ul>
            </center>
        
</div>
{% include 'AdministradorBundle:Default:administrador_footer.html.twig' %}